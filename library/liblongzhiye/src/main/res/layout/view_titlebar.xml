<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/title_bar"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/title_height"
    android:background="@color/title_bg">

    <Button
        android:id="@+id/titlebar_subtitle_left"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent"
        android:textColor="@color/white"
        android:textSize="@dimen/text_size_medium"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/titlebar_btn_left"
        android:layout_width="55dip"
        android:layout_height="@dimen/title_height"
        android:layout_alignParentLeft="true"
        android:background="@drawable/btn_transparent_selector"
        android:contentDescription="@null"
        android:padding="10dp"
        android:scaleType="centerInside"
        android:src="@mipmap/ic_back"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/titlebar_opertion_layout"
        android:layout_width="wrap_content"
        android:layout_height="@dimen/title_height"
        android:layout_alignParentRight="true"
        android:gravity="right">

        <ImageView
            android:id="@+id/titlebar_btn_right_thirdly"
            android:layout_width="55dip"
            android:layout_height="@dimen/title_height"
            android:background="@drawable/btn_transparent_selector"
            android:contentDescription="@null"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:visibility="gone" />

        <TextView
            android:id="@+id/titlebar_btn_right_second"
            android:layout_width="wrap_content"
            android:layout_height="@dimen/title_height"
            android:background="@drawable/btn_transparent_selector"
            android:gravity="center"
            android:minWidth="55.0dip"
            android:textColor="@color/black"
            android:textSize="@dimen/text_size_small"
            android:visibility="gone" />

        <ImageView
            android:id="@+id/titlebar_btn_right_first"
            android:layout_width="55.0dip"
            android:layout_height="@dimen/title_height"
            android:background="@drawable/btn_transparent_selector"
            android:contentDescription="@null"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:visibility="gone" />
        <!--android:background="@drawable/btn_transparent_selector"-->
    </LinearLayout>

    <!--<TextView
        android:id="@+id/titlebar_text_title"
        style="@style/TitlebarTextview"
        android:layout_centerInParent="true"
        android:ellipsize="end" />-->

    <TextView
        android:id="@+id/titlebar_text_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:textColor="@color/black"
        android:textSize="@dimen/text_size_medium" />

</RelativeLayout>